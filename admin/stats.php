<?php 
require 'lib.inc.php';
?>

<?php
// fonction total des réservations
function obtenirTotalReservations() {
  // requête SQL le total des réservations
  $resultat = // Exécuter votre requête SQL ici
  $total = // Traiter le résultat de la requête pour obtenir le total

  return $total;
}

// Fonction pour obtenir les réservations d'aujourd'hui
function obtenirReservationsAujourdhui() {
  // Effectuer la requête SQL pour récupérer les réservations d'aujourd'hui
  $resultat = // Exécuter votre requête SQL ici
  $reservations = // Traiter le résultat de la requête pour obtenir les réservations d'aujourd'hui

  return $reservations;
}

// Fonction pour obtenir les réservations de la semaine
function obtenirReservationsSemaine() {
  // Effectuer la requête SQL pour récupérer les réservations de la semaine
  $resultat = // Exécuter votre requête SQL ici
  $reservations = // Traiter le résultat de la requête pour obtenir les réservations de la semaine

  return $reservations;
}

// ...
?>
